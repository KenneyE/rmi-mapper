<% @features.each do |feature| %>
  <%= label_tag(feature.id, feature.name.capitalize) %>
  <%= check_box_tag("features[]",
        feature.id,
        @selected_features.include?(feature.id.to_s)
        ) %>
<% end %>
